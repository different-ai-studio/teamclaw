{
  "name": "teamclu-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "start": "node .output/server/index.mjs"
  },
  "//": "Versions are pinned exact and pnpm-lock.yaml is committed on purpose: the daemon builds this tree with `pnpm install --frozen-lockfile` before deploying to FC. A floating range once drifted @tanstack/react-start onto a release that dropped the entrypoint the template used, which broke every app build.",
  "dependencies": {
    "@tanstack/react-router": "1.170.18",
    "@tanstack/react-start": "1.168.32",
    "postgres": "3.4.9",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tanstack/nitro-v2-vite-plugin": "1.155.0",
    "@vitejs/plugin-react": "4.7.0",
    "typescript": "5.9.3",
    "vite": "6.4.3",
    "vite-tsconfig-paths": "5.1.4"
  }
}
