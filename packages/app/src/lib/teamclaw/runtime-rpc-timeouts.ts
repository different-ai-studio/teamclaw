/** Startup-path MQTT RPC timeouts (fetchWorkspaces, runtimeStart, etc.). */
export const RUNTIME_START_RPC_TIMEOUT_MS = 20_000

/** How long to wait for ActorPresence before proceeding or gating. */
export const DEVICE_PRESENCE_GATE_TIMEOUT_MS = 2_000
